fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
aiofiles>=23.0
aiosqlite>=0.20.0
pydantic>=2.0
pyyaml>=6.0

# ─── Dev dependencies ──────────────────────────────────
# Les dépendances de développement (pytest, ruff, mypy, etc.)
# sont déclarées dans pyproject.toml sous [project.optional-dependencies].
# Installe-les avec :  pip install -e ".[dev]"
#
#   pytest>=8.0
#   pytest-asyncio>=0.23
#   pytest-cov>=4.1
#   ruff>=0.4
#   mypy>=1.10
